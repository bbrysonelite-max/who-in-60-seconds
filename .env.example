# Copy this file to .env and fill in your own throwaway wallet's private key.
# Never commit .env — it is already in .gitignore.
#
# The key only needs to hold ~$1 USDC on Base mainnet (the lookup costs $0.05).
# Do not use a wallet that holds anything else.

PRIVATE_KEY=0xyour64charhexprivatekey
